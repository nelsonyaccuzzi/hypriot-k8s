---

  - name: Reset KubeAdm
    shell: kubeadm reset -f
    
  - name: Clean IP Tables
    shell: |
      iptables -F 
      iptables -t nat -F 
      iptables -t mangle -F 
      iptables -X

  - name: Reboot Node
    reboot:
